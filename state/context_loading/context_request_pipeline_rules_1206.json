{
  "id": "context_request_pipeline_rules_1206",
  "version": "1206_v3_evidence_behavior_snapshot_v3",
  "status": "single_server_snapshot_with_evidence_state_active",
  "purpose": "Build one immutable Railway context snapshot per protected turn_id, then serve only small scene-needed semantic chunks from it.",
  "pipeline": [
    {
      "step": 1,
      "name": "preflight",
      "description": "Return active state, current calendar beat, last scene summary, immediate constraints. No full cards."
    },
    {
      "step": 2,
      "name": "turn_contract_snapshot",
      "description": "getTurnContract validates the pending turn_id, selects full-card characters, scene-active memories and only relevant relationship pairs, then freezes one server-side context snapshot."
    },
    {
      "step": 3,
      "name": "required_context_manifest",
      "description": "Railway returns the ordered semantic chunk manifest from the frozen snapshot."
    },
    {
      "step": 4,
      "name": "ordered_context_chunks",
      "description": "Railway serves each approved chunk in numeric order and records progress; repeats are idempotent and skipped indices are rejected."
    },
    {
      "step": 5,
      "name": "internal_scene_draft",
      "description": "After all chunks are served, GPT drafts from that snapshot only and does not show the scene yet."
    },
    {
      "step": 6,
      "name": "apply_turn_result",
      "description": "GPT sends the same turn_id, final scene and evidence-backed proposed_updates; API rejects unsourced facts, absent-character memory and unloaded-pair changes, then atomically persists valid state and authorizes visible output."
    }
  ],
  "hard_rules": [
    "Do not return full YAML cards to Custom GPT Actions by default.",
    "There is exactly one immutable context snapshot for one pending turn_id.",
    "Client-returned turn_contract, state slices and chunk lists are never a source of truth.",
    "Do not split raw huge cards into chunks; derive bounded semantic writer-card blocks instead.",
    "POV full-card sources are mandatory; never insert Akira as a protagonist fallback.",
    "Every active NPC writer card includes goal, voice/behavior, reaction triggers, known facts and active unknowns.",
    "A directly addressed full NPC must respond somehow but never has to agree, obey, leave or disclose.",
    "Background participants may react briefly without taking focus; referenced/absent characters do not witness the scene.",
    "Static personality and voice never change through dynamic state; only mood, goals, tactics, memory and relationships may evolve.",
    "Confirmed facts, observations, heard reports, beliefs and mistakes are separate evidence buckets.",
    "Calendar, prompt, runtime rules and hidden lore never become NPC knowledge.",
    "Dynamic character memory may be written only for memory_character_ids loaded in this snapshot.",
    "Relationship pairs load only when both participants are scene-active or an active character explicitly thinks/speaks about the pair.",
    "Relationship updates are evidence-backed bounded deltas for snapshot-loaded pairs only.",
    "applyTurnResult is blocked until every required snapshot chunk was served in order.",
    "State active slice is always needed, but full state history is not.",
    "Location basic slice is needed for every scene.",
    "Schedule slice is needed for base/public/location movement scenes; schedule files may be empty placeholders until filled.",
    "Character full cards remain authoritative in repo; context slices are derived from full cards, not from separate runtime summaries.",
    "Railway may deny a requested block if calendar, location, knowledge, energy, or past triggers do not allow it."
  ]
}
