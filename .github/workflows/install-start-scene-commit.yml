name: Install start scene commit

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize

permissions:
  contents: write

jobs:
  install:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/start-scene-commit
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Integrate atomic start scene commit
        run: |
          python tools/install_start_scene_commit.py
          python tools/fix_start_scene_test_expectations.py
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt pytest httpx
      - name: Compile runtime and tests
        run: python -m compileall -q app tests
      - name: Run full regression suite
        run: |
          set +e
          PYTHONPATH=. pytest -q --tb=short > start-scene-commit-pytest.txt 2>&1
          status=$?
          cat start-scene-commit-pytest.txt
          exit $status
      - name: Upload regression diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: start-scene-commit-pytest
          path: start-scene-commit-pytest.txt
          if-no-files-found: error
      - name: Commit verified integration
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Add atomic start scene commit"
          git push origin HEAD:agent/start-scene-commit
